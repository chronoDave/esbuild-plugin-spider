{
  "name": "@chronocide/esbuild-plugin-spider",
  "version": "0.4.1",
  "type": "module",
  "types": "dist/esbuild-plugin-spider.d.ts",
  "exports": {
    ".": {
      "import": "./dist/esbuild-plugin-spider.js",
      "require": "./dist/esbuild-plugin-spider.cjs"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist/**/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chronoDave/esbuild-plugin-spider.git"
  },
  "author": "Chronocide",
  "license": "AGPL-3.0",
  "engines": {
    "node": ">=22.13.1"
  },
  "scripts": {
    "lint": "eslint src",
    "build": "rollup -c",
    "test": "node --experimental-strip-types --test src/**/*.spec.ts --test-reporter=tap",
    "preversion": "npm run lint:tsc && npm run lint && npm run test && npm run build",
    "postversion": "npm publish"
  },
  "peerDependencies": {
    "@chronocide/spider": "^0.6.0",
    "esbuild": "^0.25.0"
  },
  "devDependencies": {
    "@chronocide/eslint-config": "github:chronoDave/eslint-config",
    "@chronocide/spider": "^0.6.0",
    "@types/node": "^22.14.1",
    "esbuild": "^0.25.0",
    "eslint": "^9.9.1",
    "rollup": "^4.22.4",
    "rollup-plugin-dts": "^6.1.1",
    "rollup-plugin-esbuild": "^6.1.1",
    "typescript": "^5.5.4"
  }
}
